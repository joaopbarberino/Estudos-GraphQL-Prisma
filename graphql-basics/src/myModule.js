const message = "Yo";

const name = "João";

const location = "Brasil";

const getGreeting = (name) => `Bo toca ${name}`;

export {
    message,
    name,
    getGreeting,
    location as
    default
}
